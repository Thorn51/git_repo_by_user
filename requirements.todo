Requirements:
    o	The user must be able to search for a GitHub user handle.
    o	The search must trigger a call to GitHub's API.
    o	The repos associated with that handle must be displayed on the page.
    o	You must display the repo name and link to the repo URL.
    o	The user must be able to make multiple searches and see only the results for the current search.